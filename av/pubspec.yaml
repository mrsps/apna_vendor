name: av
description: Vendors at your doorstep.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cloud_firestore: ^2.4.0
  cloud_functions: ^3.0.2
  firebase_auth: ^3.0.1
  firebase_core: ^1.4.0
  firebase_dynamic_links: ^2.0.7
  firebase_messaging: ^10.0.6
  firebase_storage: ^10.0.1
  flutter:
    sdk: flutter
  freezed_annotation: ^0.14.2
  image_cropper: ^1.4.1
  image_picker: ^0.8.2
  json_annotation: ^4.0.1
  json_serializable: any
  pin_code_fields: ^7.2.0
  provider: ^5.0.0
  share_plus: ^2.1.4
  shared_preferences: ^2.0.6

dev_dependencies:
  build_runner: ^2.0.5
  flutter_test:
    sdk: flutter
  freezed: ^0.14.2

flutter:
  uses-material-design: true